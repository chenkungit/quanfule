//test
import { orderQuery } from '../api/api'
export const getCount = state => {
    return state.count
}

export const getToken = state => {
    return state.token
}
export const getorderInfo = state => {
    return state.vx_order_info
}